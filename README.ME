FIRST STEPS

For running this project locally, you should do this things before:
-Modify the connection string in the appsettings.json according to your credentials
-Execute the command update-database in the Package Manager Console for creating the db in your DBMS
-Build and run the project!
